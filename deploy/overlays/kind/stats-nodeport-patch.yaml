apiVersion: v1
kind: Service
metadata:
  name: gateway-stats
  namespace: tcp-lab
spec:
  type: NodePort
  ports:
    - name: http
      protocol: TCP
      port: 8404
      targetPort: stats
      nodePort: 30001
