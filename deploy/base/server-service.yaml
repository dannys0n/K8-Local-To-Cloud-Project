apiVersion: v1
kind: Service
metadata:
  name: tcp-server
  namespace: tcp-lab
spec:
  selector:
    app: tcp-server
  ports:
    - name: tcp
      protocol: TCP
      port: 7000
      targetPort: tcp
