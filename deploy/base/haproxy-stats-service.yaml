apiVersion: v1
kind: Service
metadata:
  name: haproxy-stats
  namespace: tcp-lab
spec:
  type: ClusterIP
  selector:
    app: haproxy
  ports:
    - name: http
      protocol: TCP
      port: 8404
      targetPort: stats
