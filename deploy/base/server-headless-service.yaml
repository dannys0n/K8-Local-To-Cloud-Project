apiVersion: v1
kind: Service
metadata:
  name: tcp-server-headless
  namespace: tcp-lab
spec:
  clusterIP: None
  selector:
    app: tcp-server
  ports:
    - name: tcp
      protocol: TCP
      port: 7000
      targetPort: tcp
