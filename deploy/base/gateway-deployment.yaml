apiVersion: apps/v1
kind: Deployment
metadata:
  name: gateway
  namespace: tcp-lab
spec:
  replicas: 3
  strategy:
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
  selector:
    matchLabels:
      app: gateway
  template:
    metadata:
      labels:
        app: gateway
    spec:
      terminationGracePeriodSeconds: 10
      tolerations:
        - key: node.kubernetes.io/not-ready
          operator: Exists
          effect: NoExecute
          tolerationSeconds: 15
        - key: node.kubernetes.io/unreachable
          operator: Exists
          effect: NoExecute
          tolerationSeconds: 15
      topologySpreadConstraints:
        - maxSkew: 1
          topologyKey: kubernetes.io/hostname
          whenUnsatisfiable: ScheduleAnyway
          labelSelector:
            matchLabels:
              app: gateway
      containers:
        - name: gateway
          image: tcp-gateway:dev
          imagePullPolicy: IfNotPresent
          env:
            - name: POD_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
            - name: LISTEN_ADDR
              value: ":9000"
            - name: STATS_ADDR
              value: ":8404"
            - name: SERVER_HOST
              value: tcp-server-headless
            - name: SERVER_PORT
              value: "7000"
            - name: DISCOVERY_INTERVAL
              value: "500ms"
            - name: ROUTE_TIMEOUT
              value: "10s"
            - name: BACKEND_TIMEOUT
              value: "3s"
          ports:
            - name: tcp
              containerPort: 9000
              protocol: TCP
            - name: stats
              containerPort: 8404
              protocol: TCP
          readinessProbe:
            httpGet:
              path: /healthz
              port: stats
            initialDelaySeconds: 1
            periodSeconds: 1
            timeoutSeconds: 1
            failureThreshold: 2
          livenessProbe:
            httpGet:
              path: /healthz
              port: stats
            initialDelaySeconds: 3
            periodSeconds: 1
            timeoutSeconds: 1
            failureThreshold: 3
            terminationGracePeriodSeconds: 2
          resources:
            requests:
              cpu: 25m
              memory: 32Mi
            limits:
              cpu: 250m
              memory: 128Mi
