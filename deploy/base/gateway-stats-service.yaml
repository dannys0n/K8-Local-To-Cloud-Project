apiVersion: v1
kind: Service
metadata:
  name: gateway-stats
  namespace: tcp-lab
spec:
  type: ClusterIP
  selector:
    app: gateway
  ports:
    - name: http
      protocol: TCP
      port: 8404
      targetPort: stats
