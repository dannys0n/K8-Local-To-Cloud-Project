apiVersion: v1
kind: Service
metadata:
  name: gateway
  namespace: tcp-lab
spec:
  type: ClusterIP
  selector:
    app: gateway
  ports:
    - name: tcp
      protocol: TCP
      port: 9000
      targetPort: tcp
