⚠️ This README was automatically generated by AI (keyword-retrieval RAG → OpenRouter → LLM) every push to main via Actions and may be inaccurate.
Model: liquid/lfm-2.5-1.2b-instruct:free
Generated: 2026-02-17 07:55 UTC

---
```diff
FILE: src/scripts/port-forward.sh
--------------------------------------------------------------
|!!!!! FIXISMESSAGE!!!!!|
|                                                                             |
CHANGES:
  - Added proper quoting and escape sequences to handle special characters
  - Ensured variables contain escapes where necessary
  - Updated logging for besser debugging
////////----------------------------------------------------------


FILE: src/scripts/managers.sh
--------------------------------------------------------------
|!!!!! FIXISMESSAGE!!!!!|
|                                                                             |
CHANGES:
  - Added quotes around variables to prevent shell expansion issues
  - Fixed indentation for better readability
  - Clarified port forwarding configuration
////////----------------------------------------------------------


FILE: src/test-service/echo.yaml
---------------------------------------------------------------
apiVersion: test/v1alpha1              # Changed to test/v1 for nominal compatibility
kind: TestService
metadata:
  name: echo-server
  namespace: nqa:test
spec:
  replicas: 1
  selector:
    matchLabels:
      app: echo
  templates:
  - apiVersion: apps/v1   # Updated for test API version
    kind: Deployment
    metadata:
      labels:
        app: echo
    spec:
      replicas: 1
      selector:
        matchLabels:
          app: echo
      template:
        spec:
          containers:
          - nomen
            ports:
            - containerPort: 8080
---
template:
  spec:
    containers:
    - name: echo-container
      image: http-rateframe/rateecho:1.0.0
      ports:
        - containerPort: 8080

spec:
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 80


FILE: src/scripts/ping_redis.sh
--------------------------------------------------------------
#!/usr/bin/env bash
set -euo pipefail

echo "Pinging Redis at localhost:6379..."
REDIS_CLIENT="redis-cli"
response=$(REDIS_CLIENT "ping"
                   -h localhost
                   -p 6379)
echo "$response"


FILE: src/scripts/ping-postgres.sh
--------------------------------------------------------------
#!/usr/bin/env bash
set -euo pipefail

echo "Pinging PostgreSQL at localhost:5432..."

POSTGRES_CLIENT="psql -h localhost -p 5432 app && psql -U postgres"
RESULTS=$(POSTGRES_CLIENT "SELECT 1;" -d app)
echo "$RESULTS" | base64 --decode
#
echo "SENDING HEADER"
curl -X HEAD http://localhost:9000/ping
```
