# Kubernetes manifests go here

Put your *application* deployment YAML/Helm here (Proxy, shards, databases, dashboards).

Keep `scripts/` for *cluster lifecycle* only (installing k3s, host setup, join/teardown).
