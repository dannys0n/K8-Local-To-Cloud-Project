# Copy to: config/cluster.env
#
# Required:
SERVER_IP=192.168.1.10
SERVER_SSH_USER=danny
SSH_KEY=~/.ssh/id_ed25519

# Space-separated list of worker node IPs (optional; can be empty)
WORKER_IPS="192.168.1.20"
WORKER_SSH_USER=pi

# Optional: API endpoint added to the k3s server TLS SAN.
# Defaults to SERVER_IP if omitted (simple LAN setup).
# K3S_API_ENDPOINT=k3s-api.lan

# Optional: pin versions (recommended once you're stable)
# K3S_VERSION=v1.29.8+k3s1
# K3SUP_VERSION=0.13.6

# Optional: pass extra args to the k3s server install.
# Example to disable default Traefik/ServiceLB (only if you plan to replace them):
# K3S_EXTRA_ARGS="--disable traefik --disable servicelb"
K3S_EXTRA_ARGS=""

# Optional platform installs (used by scripts/platform.sh)
INSTALL_METRICS_SERVER=true

# MetalLB (use only if you disabled k3s ServiceLB OR you want real LoadBalancer IPs on bare metal)
INSTALL_METALLB=false
# Example: a pool in your LAN that is NOT managed by DHCP
# METALLB_ADDRESS_POOL="192.168.1.240-192.168.1.250"

# Ingress controller (only if you disabled Traefik and want nginx)
INSTALL_INGRESS_NGINX=false

# cert-manager (for TLS cert automation; useful if you use Ingress)
INSTALL_CERT_MANAGER=false
